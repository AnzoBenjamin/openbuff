# Audit findings: gate-verify-b-cleared-records

- Subsystems: agents/base2, packages/agent-runtime, docs, common
- Features: cleared-file-recording, gate-rearm-on-edit, deletion-attestation-by-absence, unreadable-fail-closed, durable-gate-pass-reuse, reviewer-attestation, git-committer-commit-guard, conversation-gate-state-reuse, gate-marker-content-fingerprint
- Files covered: 7

## [LOW] api-contract — agents/base2/base2.ts:11456 — Q1 CONFIRMED: pass records path->content-marker map + pass-level fields in durable base2ActiveWork; INFERRED gap: no per-file reviewer/receipt provenance
- **Risk:** Which files were cleared is recorded precisely (path + content marker) and pass evidence is recorded at pass level and in reviewReceipts, but the per-file ledger stores no reviewer family, receipt id, verdict, or timestamp, so 'cleared on what evidence' is only reconstructable from pass-level/receipt records. Files credited outside a reviewer pass (see clean-tree prune finding) are indistinguishable from reviewed ones in the ledger.
- **Fix:** If per-file provenance is required, extend gatePassedFileMarkers values to {marker, source: 'review-receipt'|'clean-tree-prune'|'conversation-reuse'|'durable-reuse', gateId, recordedAt}, or link each credited file to the reviewReceipts entry whose reviewedFiles covers it.
- **Evidence:** Q1 fields on a pending-gate-file pass: - CONTENT MARKER CONFIRMED: creditGatePassedFiles records ONLY path->marker (agents/base2/base2.ts:11457 'const markers = (activeWorkState.gatePassedFileMarkers ??= {})', :11466-11467 'gatePassedFiles.add(file)' / 'markers[file] = marker'). Marker shape CONFIRMED at :11845-11846: 'const prefix = symlinkParts.length > 0 ? 'symlink-sha256' : 'sha256'' then 'const marker = `${prefix}:${hash.digest('hex')}:${bytesReadTotal}`' => sha256:<hash>:<byteLength> (byteLength = bytesReadTotal), matching docs/request-flow.md:252-255. - PASS-LEVEL FIELDS CONFIRMED at the reviewer pass (base2.ts:6586-6594): 'activeWorkState.gatePassedPendingFiles = passedPendingFiles' (:6587, = 'Array.from(pendingGateFiles)' at :6516), 'activeWorkState.gatePassedReviewerVerdict = reviewerFinalizationVerdict' (:6588-6589), 'gatePassedValidationSummary = validationSummary' (:6590), 'gatePassedFingerprint = buildGateFingerprint(passedPendingFiles, validationSummary)' (:6591-6594). Verdict credit is LOOKS_GOOD-only (base2.ts:10408-10415: 'Historical NON_BLOCKING durable passes fail closed: only LOOKS_GOOD'). - RECEIPT ID + REVIEWER FAMILY + VERDICT + TIMESTAMP CONFIRMED in recordSuccessfulReviewReceipt: base2.ts:10124 'const gateId = `${reviewer}:${expectedFingerprint}`', :10144-10145 'reviewer,' / 'verdict: result.verdict', :10196 'recordedAt: new Date().toISOString()', stored at :10198-10203 'activeWorkState.reviewReceipts = [...].slice(-24)' deduped by gateId. Fidelity note: :10146-10147 'snapshotFingerprint: result.snapshotFingerprint ?? expectedFingerprint' persists the REVIEWER-REPORTED fingerprint (possibly drifted); only gateId uses the gate-computed one (contrast :6672-6675 'NEVER from a reviewer-REPORTED snapshotFingerprint'). Plan-task receipts carry their own id/files/timestamp (:6709 'plan-gate:${claimedPlanTaskId}:...' and :6723-6732 with 'reviewerVerdict: passVerdict', :6731 'recordedAt: new Date().toISOString()'). - STORAGE (durable vs per-turn) CONFIRMED: all cleared-file records live in mutableAgentState.base2ActiveWork (base2.ts:1161 'const existingActiveWorkState = mutableAgentState.base2ActiveWork', :1469 'mutableAgentState.base2ActiveWork = activeWorkState'), typed at common/src/types/session-state.ts:510-511 'Runtime-owned orchestrator state that must survive message compaction.' / 'base2ActiveWork?: Record<string, unknown>' - so it survives turns AND compaction (docs/request-flow.md:325-332 'Session state persists across prompts within a conversation'). Disk persistence of RunState is INFERRED from docs/request-flow.md:330-332 ('The CLI stores the RunState from each run and passes it as previousRun'), not verified in CLI code. The ONLY per-turn store is the marker cache (base2.ts:1087-1098 'per-turn marker cache keyed by normalized path ... Bounded (250 entries), reset every turn'). A user-visible <gate-state> block is also emitted (:4535-4542) and later re-parsed from messages (:10219-10253). - PER-FILE PROVENANCE GAP (INFERRED): ledger fields (:1185-1190 'gatePassedFiles: [], gatePassedFileMarkers: {}, gatePassedPendingFiles: [], gatePassedReviewerVerdict: ...') hold no reviewer family / receipt id / verdict / timestamp per file. - Flagged risk location checked: packages/agent-runtime/src/tools/tool-executor.ts only CONSUMES the published dirty set (:2484-2489 'base2 publishes uncommittedUnvalidatedFiles (dirty working-tree files not covered by a green gate pass); refuse to stage any of them via git-committer'); it reads no content markers and records no cleared files.

## [LOW] state-mutation — agents/base2/base2.ts:1643 — Q2 CONFIRMED: re-arm on edit is enforced by three independent layers; exact marker comparison is base2.ts:1643-1647
- **Risk:** No defect found: byte drift after a pass is fail-closed in all three layers (explicit edit hook, marker-eviction ledger, fingerprint reopen). Recorded as a verified conclusion for the audit trail.
- **Fix:** None required. Keep the three checks in sync if gatePassedFileMarkers' value shape ever changes.
- **Evidence:** Q2 re-arm-on-edit, all CONFIRMED: - EXACT COMPARISON CODE: agents/base2/base2.ts:1636-1652 - ':1637 const storedMarker = ledgerMarkers[file]', ':1638 const currentMarker = readGateFileContentMarker(file)', then ':1643-1647 if (storedMarker === undefined || storedMarker !== currentMarker || !isCreditableContentMarker(currentMarker))'. On mismatch the credit is evicted AND the file re-added to the pending set: ':1648 gatePassedFiles.delete(file)', ':1649 delete ledgerMarkers[file]', ':1650-1652 changedFiles.add(file) / pendingGateFiles.add(file) / evictedGatePassedFiles.push(file)', phase reset :1658 'currentPhase = 'awaiting_validation''. Intent comment :1626-1628: 'evict ANY credited file whose current content marker no longer matches the marker captured when it was credited into gatePassedFiles'. - EXPLICIT EDIT PATH: recordChangedFiles base2.ts:9123-9125 'pendingGateFiles.add(file)' / 'gatePassedFiles.delete(file)' and :9130-9132 'delete activeWorkState.gatePassedFileMarkers[file]' ('A re-edited file leaves the gate-passed ledger; drop its marker'); :9176-9180 wipes gatePassedPendingFiles/Verdict/ValidationSummary/Fingerprint when the edited file was in the passed set. External dirt is funneled through the same path via rearmGateForUnreviewedDirty (:11521-11523 'recordChangedFiles(unreviewed)'). - FINGERPRINT REOPEN: base2.ts:1593-1623 - if 'hasFreshGateFingerprintForPendingFiles(activeWorkState.gatePassedPendingFiles, ...)' fails, :1604-1611 re-pends every file in the passed set and deletes its orphan marker, :1614-1617 clears the four pass fields, message :1619-1620 'Previously reviewed files changed after the gate passed; validation and review were reopened.' Marker drift changes the files-v4 fingerprint because :11575-11578 maps each file to 'readGateFileContentMarker(file)'. - Comparison semantics: exact string equality over sha256:<hash>:<byteLength> (:11845-11846); a legacy credited file with NO stored marker is evicted fail-closed (:1644 'storedMarker === undefined ||', comment :1628-1631 'so a legacy ledger cannot grant an unattested commit').

## [LOW] correctness — agents/base2/base2.ts:10366 — Q3 CONFIRMED: deletion contract matches code clause-by-clause - 'missing' marker, creditable attest-by-absence, stays credited, evicted on reappearance
- **Risk:** No defect found: all four documented clauses (docs/request-flow.md:258-263) verified in code. Recorded as a verified conclusion for the audit trail.
- **Fix:** None required.
- **Evidence:** Q3 deleted-file clauses: - CLAUSE 1 (stable marker 'missing') CONFIRMED: agents/base2/base2.ts:1086 'const GATE_FILE_MISSING_CONTENT_MARKER = 'missing''. Produced for true absence only: :11767-11771 lstat probe 'if ((probeError as NodeJS.ErrnoException).code === 'ENOENT') { return GATE_FILE_MISSING_CONTENT_MARKER' (deliberately lstatSync, not existsSync: :11763-11766 'existsSync follows a dangling symlink ... and would wrongly report `missing`'), and the read catch :11873 'if (code === 'ENOENT') return GATE_FILE_MISSING_CONTENT_MARKER'. Present-but-unreadable stays unreadable:* (:11758-11762). - CLAUSE 2 (creditable + attest-by-absence) CONFIRMED: isCreditableContentMarker :10366-10370 'return (isAttestableContentMarker(value) || value === GATE_FILE_MISSING_CONTENT_MARKER)'; creditGatePassedFiles :11460-11465 'A `missing` marker (a file deleted in the changeset) is credited as-is' then 'gatePassedFiles.add(file); markers[file] = marker'. Attestation side: collectDeletedFilesFromSnapshotDetails :11601-11602 'if (line.slice(tabIndex + 1) === GATE_FILE_MISSING_CONTENT_MARKER) { deletedFiles.push(...)'; gate-reviewer.ts:273-277 'Files deleted in the changeset carry a `missing` content marker and cannot be read by the reviewer, so they are attested-by-absence and excluded from the missing computation', enforced by the missing computation at gate-reviewer.ts:283-287 '(file) => file.length > 0 && !reviewed.has(file) && !deleted.has(file)'. Only exact 'missing' counts: base2.ts:11587-11588 'Only exact `missing` markers count: `unreadable:<code>` is a present-but-unreadable file that must still be attested (fail closed)'. A deletions-only changeset still needs an attestable fingerprint (gate-reviewer.ts:276-277 + :296-311). - CLAUSE 3 (stays credited) CONFIRMED: eviction guard retains credit while stored 'missing' === current 'missing' (:1643-1647, both creditable via :10369), and a deleted file is not dirty so collectUnreviewedDirtyReviewableFiles (:11500-11505) never re-arms it - matching docs/request-flow.md:259-261 'a stable `missing` marker stays credited instead of re-arming the gate on every loop'. - CLAUSE 4 (evicted if the file REAPPEARS) CONFIRMED: recreation makes the current marker a present 'sha256:<hash>:<len>' (:11845-11846) which no longer equals stored 'missing' => :1645 'storedMarker !== currentMarker' => :1648-1652 evict + 'pendingGateFiles.add(file)' (re-review). Matches docs/request-flow.md:261-263 exactly. - Related: all-missing findings are pruned at :1340-1344 'findingFiles.every((file) => readGateFileContentMarker(file) === GATE_FILE_MISSING_CONTENT_MARKER)'.

## [MEDIUM] error-handling — agents/base2/base2.ts:6585 — Q4 PARTIAL: unreadable/missing-crypto fail-closed CONFIRMED for the per-file credit ledger, but INFERRED doc disagreement - turn-green and durable reuse are NOT blocked by unreadable pending files (docs say they are)
- **Risk:** docs/request-flow.md:264-267 promises 'the gate refuses to mark the turn green' for unreadable files. The per-file ledger indeed refuses credit, but finalization (final_response_allowed) and the durable gatePassedFingerprint can still be granted over a snapshot containing unreadable:<code> markers: reviewer coverage is name-only and v3 fingerprint attestability is shape-only. A stable unreadable error string then round-trips as durable pass identity and the turn goes green with an unreadable file never byte-attested.
- **Fix:** Before setting final_response_allowed, assert every pending file's marker satisfies isCreditableContentMarker (base2.ts:10366); alternatively make hashGateSnapshotDetails non-attestable when any component marker is non-attestable, and/or require reviewedFiles entries to carry per-file content markers.
- **Evidence:** Q4 unreadable / missing-crypto: - PRODUCERS CONFIRMED: readGateFileContentMarkerUncached returns 'unreadable:<code>' for every failure mode - :11730 'unreadable:no-module-loader', :11738 'unreadable:no-cwd', :11746 'unreadable:outside-project', :11775 'return 'unreadable:lstat-failed'', :11790/:11793/:11796 'not-a-directory'/'not-a-file', :11812 'unreadable:outside-project-symlink', :11834 and :11843 'unreadable:changed-during-read', :11874 'return `unreadable:${code}`'. Missing crypto: agents/base2/gate-fingerprint.ts:38-40 'Fail closed: without a collision-resistant hash ... return 'unreadable:no-crypto''. - NO DURABLE PER-FILE CREDIT CONFIRMED: isAttestableContentMarker :10354-10356 '/^(?:symlink-)?sha256:[a-f0-9]{64}:\d+$/' (comment :10353 'Unreadable/missing/error markers are never attestable'); isCreditableContentMarker :10366-10371 accepts only that or 'missing'; creditGatePassedFiles :11465 'if (!isCreditableContentMarker(marker)) continue' (comment :11463-11464 'unreadable files, and missing-crypto states produce non-attestable markers that must never enter the durable ledger'). Prior credit is evicted when the file turns unreadable: :1646 '!isCreditableContentMarker(currentMarker)'. Sentinels are never cached: :11623-11625 'Error/sentinel markers (unreadable:*, missing) are NOT cached ... a transient failure state must not pin stale credit'. - REUSE FAIL-CLOSED CONFIRMED: hasFreshGateFingerprintForPendingFiles :10385-10394 rejects a missing/non-attestable recorded fingerprint (:10387-10389 'Non-attestable fingerprints (unreadable:no-crypto, etc.) can never be reused as durable gate credit') and requires recomputed equality (:10395 'return recorded === currentFingerprint'); a readable->unreadable transition changes the files-v4 line (:11575-11579) so the fingerprint stops matching. - INFERRED DISAGREEMENT with docs/request-flow.md:264-267 ('still **fail closed**: the gate refuses to mark the turn green rather than silently treating an absent or unreadable file as passing, and never grants durable credit for a non-creditable marker'): (a) reviewer coverage is NAME-only - gate-reviewer.ts:283-287 compares normalized path names, and an unreadable-but-present file must be listed by name (base2.ts:11587-11588), so a name listing satisfies coverage without ever reading the bytes; (b) finalization gates only on 'if (passedPendingFiles.length > 0 && reviewerFinalizationVerdict)' (base2.ts:6560) and calls creditGatePassedFiles (:6585) whose silent skip (:11465) is never re-checked before :6630-6638 'currentPhase = 'final_response_allowed'' / 'finalResponseGateOpen = true'; (c) fingerprint attestability is shape-only - gate-fingerprint.ts:15-17 '/^v3:[a-f0-9]{64}$/' while :36-37 returns 'v3:' + sha256(details) for ANY details, so details containing 'unreadable:EACCES' lines still hash to an attestable fingerprint that :10390-10395 will accept on reuse while the error string is stable. 'never grants durable credit for a non-creditable marker' is TRUE only for the gatePassedFileMarkers ledger. Marked INFERRED: the main reviewer-pass path (:6560-6639) is verified; bypass/hook-skip finalization branches were not exhaustively enumerated.

## [MEDIUM] correctness — agents/base2/base2.ts:1951 — Q5 GAP (CONFIRMED code path): clean-tree prune credits pending files into the cleared ledger with NO reviewer attestation
- **Risk:** A pending file that merely becomes clean in git status (committed by an external process while the gate is pending, or reverted) is credited as gate-passed without any reviewer attestation of its bytes and dropped from the pending set, so it is never reviewed and can never re-enter gate scope (deriveGateScopeFiles filters !gatePassedFiles.has(file)). The ledger then asserts 'cleared' for content no reviewer saw, and its commit-guard exemption follows. docs/request-flow.md:248-267 never mentions this credit path.
- **Fix:** Credit clean-tree-pruned files under an explicit non-review source (or leave them out of gatePassedFiles and rely on clean-tree status for commit UX) and document the path; if review coverage is required, keep them pending until a reviewer attests or record them as 'excluded: committed-before-review'.
- **Evidence:** Q5 gap-hunt, credit-without-evidence: - CONFIRMED CODE PATH: agents/base2/base2.ts:1943-1953 - guard 'isRealGitStatusResult && gitStatusObservedDirty && gitStatusFiles.length === 0', then per previously-dirty pending file: ':1950 pendingGateFiles.delete(pendingFile)' and ':1951 creditGatePassedFiles([pendingFile])'. No reviewer tool result, no fingerprint, no attestation is consulted anywhere in this block. Intent comment :1935-1937: 'Prune pending gate files that were previously observed as dirty in git status but are no longer present (i.e., they were committed).' Purpose is UX (:1937-1939 'committed files stay in the pending set forever ... keeping the gate in 'awaiting_validation''), but the mechanism writes the SAME cleared-file record a reviewed pass writes (:11456-11468). - Consequence: creditGatePassedFiles records the CURRENT marker (:11459 'const marker = readGateFileContentMarker(file)'), so the eviction ledger (:1643-1653) protects against later drift but cannot detect that the credited bytes were never attested (the marker binds bytes, not evidence). The file also leaves gate scope permanently: :11538-11542 'deriveGateScopeFiles ... .filter((file) => taskRelatedFiles.has(file) && !gatePassedFiles.has(file))'. - Every OTHER credit path is attestation-backed (checked): reviewer pass (:6585 runs after collectReviewerAttestationIssues at :5366-5371 over 'reviewableGateScopeFiles, reviewDeletedFiles'), conversation reuse (:4502 runs only after :4488-4491 fingerprint freshness), durable reuse (:4555 'hasDurableGatePassForPendingFiles'). This prune is the only unattested credit path found. - Reachability INFERRED: requires an external commit/revert mid-turn (git-committer spawns are blocked while the gate is pending per docs/request-flow.md:236-241 'This guard has no bypass'), but user/other-process commits are unrestricted.

## [MEDIUM] api-contract — agents/base2/gate-reviewer.ts:304 — Q5 GAP (CONFIRMED): fingerprint drift is tolerated when file NAMES are covered, enabling stale-byte credit - contradicts docs/request-flow.md:273-274 'A mismatch or omitted file is blocking'
- **Risk:** A reviewer that names every pending file but attested stale bytes passes the attestation gate: a mismatched v3 fingerprint blocks only when a file-coverage gap also exists. Credited markers then bind to whatever bytes are on disk at finalization (live re-read), so a file can be cleared without any attestation covering its CURRENT bytes; the drift is recorded as telemetry only. This is a documented-contract disagreement: docs say a mismatch is blocking.
- **Fix:** Either restore strict fingerprint equality in collectReviewerAttestationIssues (block on any attestable mismatch), or amend docs/request-flow.md:273-274 to state the drift tolerance and strengthen reviewedFiles to carry per-file content markers so coverage is byte-bound rather than name-bound.
- **Evidence:** Q5 gap-hunt, stale credit / cross-file credit: - TOLERANCE CONFIRMED: agents/base2/gate-reviewer.ts:304-307 '} else if (reportedFingerprint !== expectedFingerprint && missing.length > 0) {' -> ':309 BLOCKING: reviewer snapshot fingerprint did not match the reviewed working tree' - the mismatch branch fires ONLY when a coverage gap also exists. Rationale comment gate-reviewer.ts:289-291: 'a coverage-complete review reporting a well-formed v3 fingerprint is trusted even when the exact snapshot id advanced between its spawn and attestation; only a FILE-COVERAGE gap or a missing / non-attestable fingerprint stays a hard blocker' (:292-294: drift is merely 'record[ed] ... via `collectReviewerFingerprintDrift`'). base2 callers agree: base2.ts:3501-3506 'accepted even when the exact snapshot id advanced; only genuine file-coverage gaps or a missing/non-attestable fingerprint require a re-spawn'; :3611 'Fingerprint-only drift on a fully-attesting review is NOT a [blocker]'; :6672-6675 'the attestation path deliberately drift-tolerates'. - COVERAGE IS NAME-ONLY CONFIRMED: gate-reviewer.ts:283-287 'const missing = pendingFiles .map((file) => normalizeGateFilePath(file)) .filter((file) => file.length > 0 && !reviewed.has(file) && !deleted.has(file))' - reviewedFiles is a path-name list; StructuredReviewerOutput carries no per-file content markers. - STALE-CREDIT CONSEQUENCE: at finalization the ledger binds CURRENT bytes, not attested bytes - base2.ts:11459 'const marker = readGateFileContentMarker(file)' inside creditGatePassedFiles called at :6585, and :6591-6593 'gatePassedFingerprint = buildGateFingerprint(passedPendingFiles, validationSummary)' recomputed post-hoc. So drift between reviewer snapshot and finalization is silently absorbed into 'cleared' records. - DOC DISAGREEMENT CONFIRMED: docs/request-flow.md:273-274 'Versioned reviewer results must echo the exact snapshot fingerprint and attest to every pending file. A mismatch or omitted file is blocking.' vs the code above (mismatch tolerated when 'missing.length === 0'). - CREDIT FROM A DIFFERENT FILE'S ATTESTATION: CLOSED (verified clean) - entry-scoped resolution binds fingerprint AND coverage to one attesting entry (base2.ts:7952-7955 'snapshotFingerprint: attesting?.snapshotFingerprint, reviewedFiles,'; call anchor gate-reviewer.ts:263 'const result = resolveReviewerAttestation(structured, expectedFingerprint)'); a receipt with no attestable fingerprint fails closed (:296-303 'BLOCKING: reviewer did not report an attestable snapshot fingerprint'), and any unlisted pending file lands in 'missing' and blocks (:312-315 'BLOCKING: reviewer did not attest to every pending file: ...').

## [LOW] correctness — agents/base2/base2.ts:11459 — Q5 GAP (INFERRED): delete->recreate between attestation and crediting yields credit for bytes no reviewer attested (TOCTOU)
- **Risk:** A path that is 'missing' at review time is attested-by-absence and excluded from coverage; if it is recreated before creditGatePassedFiles runs, the ledger records a sha256 marker for the NEW bytes (read live at credit time) while the reviewer attested their ABSENCE. The eviction guard then keeps that credit because stored equals current. The window is narrow (review -> finalization) but no evidence links the credited bytes to any attestation.
- **Fix:** Capture content markers when the review snapshot is built and credit from those captured markers (fail closed on any drift between snapshot and credit), instead of re-reading live bytes inside creditGatePassedFiles.
- **Evidence:** Q5 gap-hunt, deletion TOCTOU: - The deleted set handed to the attestation check comes from the REVIEW-TIME snapshot: base2.ts:5366-5371 'collectReviewerAttestationIssues(reviewerToolResult, reviewSnapshotFingerprint, reviewableGateScopeFiles, reviewDeletedFiles)' where reviewDeletedFiles derives from collectDeletedFilesFromSnapshotDetails (:11591-11606) over the snapshot built at review spawn. - Credit re-reads LIVE bytes afterwards: base2.ts:11457-11467 ':11459 const marker = readGateFileContentMarker(file)' then ':11467 markers[file] = marker', invoked at :6585 AFTER the review resolved. A file recreated in that window yields a present 'sha256:<hash>:<len>' marker (:11845-11846) that no reviewer attested (it attested absence via gate-reviewer.ts:273-287). - The marker-eviction guard cannot catch it: stored === current (both computed post-recreation), and isCreditableContentMarker('sha256:...') is true (:10366-10371), so :1643-1647 retains the credit. - Same live-read pattern backs the post-pass fingerprint (:6591-6593), so the 'durable evidence' of the pass equally describes post-attestation bytes. Marked INFERRED: code path verified; no runtime reproduction was run (read-only audit).

## [LOW] state-mutation — agents/base2/base2.ts:11641 — Q5 GAP (INFERRED): marker cache fast-path is size+mtime keyed, so an equal-size same-mtime rewrite serves a stale content marker
- **Risk:** readGateFileContentMarker's cache returns the previously computed marker when stat.size and stat.mtimeMs match. A rewrite preserving both (mtime-granularity races, deliberate touch -r restores) evades every downstream marker comparison - eviction, fingerprint freshness, and security/specialist freshness would all compare against a stale sha256 and treat drifted bytes as cleared. The uncached body hashes real bytes, so only the fast path is affected.
- **Fix:** Add an inode/ctime component to the cache key, or bypass the fast path for gate-critical comparisons; at minimum document the size+mtime trust bound next to isCreditableContentMarker.
- **Evidence:** Q5 gap-hunt, cache staleness: - CONFIRMED fast path: agents/base2/base2.ts:11636-11646 'const stat = cacheFs.statSync(cacheFs.realpathSync(...))' then ':11641-11646 if (stat.isFile() && stat.size === cached.size && stat.mtimeMs === cached.mtimeMs) { return cached.marker }'. Comment :11656-11660 admits the scheme: 'Cache only present-file sha256 markers, keyed with the stat ... Simplest scheme that preserves bytes-unchanged liveness'. - Consumers that would be fooled: eviction comparison :1638 ':1645 storedMarker !== currentMarker', fingerprint building :11575-11578, security/specialist freshness :2761-2763 and :7433-7435. All call readGateFileContentMarker and would receive the stale cached marker. - Bounds (verified): only real sha256 markers are cached (sentinels recompute every call, :11623-11625), entries are stamped from the same fd that produced the digest (:11847-11858), the cache is per-turn (:1087-1098) and capped at 250 (:1099, :11859-11863). Exploitation requires a rewrite with identical size AND identical mtimeMs - narrow and mostly adversarial. Marked INFERRED (no runtime reproduction; read-only audit).

## [LOW] api-contract — agents/base2/base2.ts:10342 — Q5 NOTE (INFERRED): conversation gate-reuse sources its verdict by regex from arbitrary message text, bounded only by the durable fingerprint precondition
- **Risk:** getConversationGatePassForPendingFiles scrapes <gate-state> JSON out of ANY message text and accepts the verdict via a bare regex on the details string, so a prompt-injected or quoted gate-state block can supply the LOOKS_GOOD verdict for the reuse credit path. Damage is bounded - reuse still requires the durable gatePassedFingerprint for the identical file set - but verdict provenance is weaker than the receipt ledger's gateId binding.
- **Fix:** Require the reuse verdict to come from reviewReceipts (gateId-bound) rather than message text, or embed the receipt id in the <gate-state> block and verify it before crediting.
- **Evidence:** Q5 gap-hunt, conversation-reuse provenance: - Verdict from message text CONFIRMED: base2.ts:10219-10253 getConversationGatePassForPendingFiles walks messages (collectMessageText :10300-10322 reads every string in every message), :10231-10248 accepts a block whose 'gateState.gate !== 'validation/reviewer'' check passes and whose 'extractReviewerVerdictFromGateDetails(gateState.details)' equals 'LOOKS_GOOD'. That extractor is a bare regex: :10342-10344 'if (/\bLOOKS_GOOD\b/.test(details)) return 'LOOKS_GOOD''. The file set is also text-scraped: :10324-10337 'details.match(/\bpending files\s*:\s*([^;\n]+)/i)'. - The credit path then trusts it (base2.ts:4473-4506): ':4493-4494 const conversationReviewerVerdict = conversationGatePass.reviewerVerdict || 'LOOKS_GOOD''' and ':4505-4506 activeWorkState.gatePassedReviewerVerdict = conversationReviewerVerdict' after ':4502 creditGatePassedFiles(gateScopeFiles)'. - BOUND (why this is LOW, INFERRED no standalone gap): the same block requires ':4488-4491 hasFreshGateFingerprintForPendingFiles(gateScopeFiles, conversationValidationSummary)', which needs the REAL durable gatePassedFingerprint for the identical file set (:10378-10395: set equality 'gateFileSetsEqual(files, activeWorkState.gatePassedPendingFiles)', attestable recorded fingerprint, recomputed equality). A forged block alone cannot conjure credit; it can only re-announce a pass whose content-identity record already exists. Malformed blocks are ignored (:10291-10294 'only explicit valid JSON can prove a prior pass').

## Coverage receipt

### Subsystems
- agents/base2
- packages/agent-runtime
- docs
- common

### Features
- cleared-file-recording
- gate-rearm-on-edit
- deletion-attestation-by-absence
- unreadable-fail-closed
- durable-gate-pass-reuse
- reviewer-attestation
- git-committer-commit-guard
- conversation-gate-state-reuse
- gate-marker-content-fingerprint

### Files
- agents/base2/base2.ts
- agents/base2/gate-paths.ts
- agents/base2/gate-reviewer.ts
- agents/base2/gate-fingerprint.ts
- docs/request-flow.md
- common/src/types/session-state.ts
- packages/agent-runtime/src/tools/tool-executor.ts

### Domains
- api-contract
- correctness
- state-mutation
- error-handling
