# Audit findings: gate-verify-c-incremental

- Subsystems: base2-handleSteps-gate, gate-reviewer-attestation, gate-state-ledgers, gate-runtime-docs
- Features: incremental-gate-submission, reviewer-attestation, already-attested-ledger, new-file-absorption, committed-prune-credit, gate-clear-reset, review-receipt-skip
- Files covered: 5

## [LOW] api-contract — agents/base2/base2.ts:4468 — Q1 CONFIRMED — incremental submission: each gate cycle submits only files changed/re-armed since the last clear plus new files (answer a), narrowed further to drifted subsets for security/specialists
- **Risk:** If the lists were (b) all dirty/uncommitted files, reviewer work would grow O(files x cycles) and already-cleared unchanged files would churn every cycle. Actual construction: per-cycle scope = dirty task files NOT in gatePassedFiles UNION currentPendingGateFiles; cleared files are excluded by the gatePassedFiles filter, so unchanged already-cleared bytes never re-enter after a clear. Precision: incremental is ACROSS clears — inside one cycle, each repair round resubmits that cycle's whole pending set (by design, re-review after repair).
- **Fix:** None required for the core contract. Optional parity hardening: pass params.changed_files / snapshot_fingerprint on the final code-reviewer spawn the way security does (base2.ts:2885-2888), so its file list is machine-bound to the spawn instead of prompt text plus attestation.
- **Evidence:** Scope freeze base2.ts:4468-4472: 'const dirtyGateScopeFiles = deriveGateScopeFiles(gitStatusFiles)' / 'const gateScopeFiles = normalizeGateFileList([...dirtyGateScopeFiles, ...currentPendingGateFiles])'. Delta filter base2.ts:11540-11541: 'return normalizeGateFileList(dirtyFiles).filter((file) => taskRelatedFiles.has(file) && !gatePassedFiles.has(file))', with comment 11533-11535: 'Already-credited (gatePassedFiles) dirty files stay out of gate scope ... must not re-arm validation/review.' run_file_change_hooks: base2.ts:4751-4752 'toolName: \'run_file_change_hooks\', input: { files: gateScopeFiles }' (initial) and 5013-5015 / 6253-6255 'input: { files: Array.from(pendingGateFiles) }' (post-repair re-verify). code-reviewer: base2.ts:5336 '`Gate-scope changed files: ${reviewableGateScopeFiles.join(\', \') || \'(unknown)\'}`' where reviewableGateScopeFiles = selectReviewableGateFiles(gateScopeFiles) (5173), attested against the same list (5366-5371). security-reviewer: base2.ts:2859-2862 'const securitySpawnFiles = securityDriftedFiles.length > 0 ? securityDriftedFiles : securityChangedFiles', with securityChangedFiles = security-sensitive reviewable subset of currentPendingGateFiles (2723-2729) and drifted subset at 2852-2857; spawned as 'changed_files: securitySpawnFiles' (2886). specialists: base2.ts:3352-3358 'const scopedFiles = driftedFiles.length > 0 ? driftedFiles : selectReviewableGateFiles(selectAuxRelevantFiles(currentPendingGateFiles))' into specialistScopedFileSets (3463-3466 spawn, 3507-3513 attestation).

## [LOW] api-contract — agents/base2/base2.ts:7042 — Q2 CONFIRMED — 'Already attested LOOKS_GOOD...' lines are review-DEPTH guidance only: listed files stay REQUIRED in reviewedFiles; entry-scoped attestation forbids exemption by prior receipts; bytes-changed files must be re-listed every round
- **Risk:** If the ledger text exempted files from attestation, a stale earlier-round receipt could cover bytes that changed after that round (missed re-review). Actual semantics: the block only shifts review depth; the spawn still demands full coverage and collectReviewerAttestationIssues has NO already-attested exemption in its missing-file computation, so omission of any non-deleted pending file is BLOCKING every round. Entry-scoping (both snapshotFingerprint and reviewedFiles from ONE attesting entry) closes the forged-receipt splice where a quoted prior receipt lends coverage to a real entry.
- **Fix:** None required. Optional: the ledger sentence could restate that listed files must still be re-attested this round (already stated at base2.ts:5352).
- **Evidence:** Generated in buildReviewerRoundLedgerLines: base2.ts:7042 'Already attested LOOKS_GOOD by an earlier round of this reviewer family:' and 7051 '`  - attested in a prior round; bytes may have changed since: ${priorList.join(\', \')}`'; the unchanged/prior split is per-RECEIPT fingerprint equality (7027-7029: 'options.currentFingerprint === receipt.snapshotFingerprint'). Effect is depth-only: 7063 'These files were already attested LOOKS_GOOD; focus your review depth on files NOT listed above (unattested or drifted). Still report any issue you find anywhere, including in the listed files.' Same spawn still requires full coverage: base2.ts:5352 '...still attest to every pending file in reviewedFiles.' and 5354 'list every pending changed file in reviewedFiles (including tests)'. Enforcement has no exemption: gate-reviewer.ts:283-287 'const missing = pendingFiles ... !reviewed.has(file) && !deleted.has(file)' (only deleted files are attested-by-absence, 273-277) and 312-315 '`BLOCKING: reviewer did not attest to every pending file: ${missing.join(\', \')}`'. Entry-scoped fail-closed resolution: gate-reviewer.ts:181-193 'when an ATTESTING entry exists, BOTH attestation fields come from it — not a per-field composite' then 'const attesting = matching ?? attestable; const reviewedFiles = attesting ? (attesting.reviewedFiles ?? []) ...'. Bytes changed since an earlier round: the file must still be re-attested in THIS round's reviewedFiles (checked against the full reviewableGateScopeFiles at 5366-5371), and the receipt must carry an attestable fingerprint (missing/non-attestable is always blocking, gate-reviewer.ts:302-306); exact byte-match binding is drift-tolerated (gate-reviewer.ts:289-295), so re-attestation is required at file-coverage level.

## [LOW] correctness — agents/base2/base2.ts:9122 — Q3 CONFIRMED — brand-new files are added to the pending set and submitted (recordChangedFiles on tool edits, message edits, and absorbed git-status entries, incl. untracked paths); untracked dirs and ownership-less foreign files excluded by design
- **Risk:** A gap here would let newly created code land unreviewed. All creation routes feed recordChangedFiles -> pendingGateFiles -> gateScopeFiles -> hooks and reviewer lists. Deliberate exclusions: untracked-DIRECTORY porcelain entries, and new dirty paths with no task relation and no selfMutatedPaths ownership (concurrent-instance isolation), plus non-reviewable classes (docs/config) which are surfaced as excluded rather than silently skipped.
- **Fix:** None required; basher/codegen-created files must publish touchedPaths/selfMutatedPaths or they stay out of the gate — documented at docs/agents-and-tools.md:526.
- **Evidence:** recordChangedFiles base2.ts:9122-9124: 'if (!pendingGateFiles.has(file)) discoveredNewPendingFile = true / changedFiles.add(file) / pendingGateFiles.add(file)'. Creation routes: tool edits 1866-1872 ('const files = extractChangedFiles((stepResult as any) && (stepResult as any).toolResult)' -> 'recordChangedFiles(files)'), message-history edits 1893-1896 ('recordChangedFiles(messageFiles)'), git-status absorb 1979-1989 ('shouldAbsorbGitStatusFile({... selfMutatedPaths })' -> 'recordChangedFiles([file], { fromStatusObservation: true })'), repair writes 5008/6190, turn-start/iteration re-arm 11523. Untracked files parse in: base2.ts:11927-11932 parseGitStatusLine 'const pathPart = trimmed.slice(2).trim()' resolves any non-## porcelain line (incl. '?? newfile'); only 11934-11936 'Untracked-directory git status entries ... must not become gate files' ('if (resolved.endsWith(\'/\')) return \'\'') are dropped. Submission via 4469-4472 union of dirtyGateScopeFiles and currentPendingGateFiles. Ownership filter: agents/base2/gate-concurrency.ts:26-30 ('if (initialGitStatusFiles.includes(file)) return false / if (gatePassedFiles.has(file)) return false / if (taskRelatedFiles.has(file)) return true / if (... selfMutatedPaths.has(file)) return true'); non-reviewable dirty task files routed to collectNonReviewableDirtyTaskFiles (base2.ts:11511-11517).

## [MEDIUM] state-mutation — agents/base2/base2.ts:1948 — Q4(a) PARTIAL HIT — clean-tree committed-prune credits pending, never-reviewed files into gatePassedFiles without any review, silently dropping them from every later submission; all other drop vectors are defended
- **Risk:** A file changed since the last clear that is still pending (never reviewed) and then committed — e.g. by raw terminal `git commit`, which is NOT covered by the git-committer gate-not-green guard — is deleted from pendingGateFiles and credited as gate-passed with zero reviewer attestation. deriveGateScopeFiles then excludes it (gatePassedFiles filter), so no later cycle's submission ever contains its bytes while the gate reports PASSED. Everything else in failure mode (a) is defended: byte drift on credited files is evicted and re-pended, unreviewed dirty files re-arm every iteration, and scope/byte changes during or after review reopen the gate.
- **Fix:** On the clean-tree prune, do not creditGatePassedFiles for pending files lacking a matching LOOKS_GOOD reviewReceipt: route them into the opt-in committed-surface review (base2.ts:2034+) or keep them pending as committed-unreviewed so the commit guard still blocks. INFERRED exploitability (read-only audit, no tests run): requires committing pending files mid-turn through a path other than the guarded git-committer spawn (docs/request-flow.md:305-312).
- **Evidence:** base2.ts:1935-1953: comment 'Prune pending gate files that were previously observed as dirty in git status but are no longer present (i.e., they were committed).' then 'for (const pendingFile of Array.from(pendingGateFiles)) { if (gitStatusObservedFiles.has(pendingFile)) { pendingGateFiles.delete(pendingFile); creditGatePassedFiles([pendingFile]) } }', gated on 'isRealGitStatusResult && gitStatusObservedDirty && gitStatusFiles.length === 0' (1943-1947). creditGatePassedFiles base2.ts:11466-11467 'gatePassedFiles.add(file); markers[file] = marker' — durable credit with NO reviewReceipt requirement. Downstream drop: base2.ts:6516 'const passedPendingFiles = Array.from(pendingGateFiles)' no longer lists it, and 11540-11541 excludes gatePassedFiles from scope. Defended vectors (contrast): marker eviction 1644-1651 'storedMarker !== currentMarker ... gatePassedFiles.delete(file) ... pendingGateFiles.add(file)'; re-arm 11521-11528 'recordChangedFiles(unreviewed)'; post-hook scope reopen 5162-5171 'The task-related dirty scope changed during validation; validation and review were reopened.'; final frozen check 6535-6550 'The frozen gate scope or file bytes changed after review; validation and review were reopened.' (re-hashes reviewed bytes at 6529-6534).

## [LOW] performance — agents/base2/base2.ts:11540 — Q4(b) REFUTED (CONFIRMED against) — unchanged already-cleared files are never re-submitted across cycles; per-cycle reviewer work is O(files changed since the last clear), not O(files x cycles)
- **Risk:** Were (b) true, n=9000 cycles over F files would cost O(F x n) reviewer/hook work and re-attest cleared bytes forever. Actual: every clear empties the pending set and credits files into gatePassedFiles (excluded from scope), identical family+fingerprint+file-set reviews are skipped outright via the bounded reviewReceipts ledger, and the already-attested prompt ledger is capped. Remaining re-work is bounded and intentional: repair rounds inside one cycle resubmit only that cycle's pending set.
- **Fix:** None required.
- **Evidence:** Clear+credit base2.ts:6581-6585 'pendingGateFiles.clear(); activeWorkState.pendingGateFiles = [] ... creditGatePassedFiles(gateScopeFiles)' (conversation reuse path 4496/4502/4513). Exclusion base2.ts:11540-11541 '!gatePassedFiles.has(file)'. Whole-skip for identical reviewed bytes: 5221-5228 'const expectedReceiptGateId = `${requiredReviewerAgentType}:${reviewableFingerprint}`' and 'receipt.gateId === expectedReceiptGateId && receipt.reviewedFileCount === reviewableGateScopeFiles.length && gateFileSetsEqual(receipt.reviewedFiles, reviewableGateScopeFiles)', surfaced as 'reviewer skip: reviewable source set unchanged since last review' (5282). Bounded ledgers: base2.ts:10198-10203 reviewReceipts '...].slice(-24)'; prompt ledger 'at most 12 files per list' (7008-7010, 'slice(0, 12)' at 7036-7039). In-cycle re-submission is post-repair by design: 5013-5015 / 6253-6255 'input: { files: Array.from(pendingGateFiles) }'. Validation side reuses receipts when fileMarkers match (docs/agents-and-tools.md:522).

## [LOW] state-mutation — agents/base2/base2.ts:6581 — Q4(c) REFUTED (CONFIRMED against) — the pending set resets at every gate clear and shrinks on commit-prune; only non-submission ledgers grow monotonically, bounded by distinct task files not by cycles
- **Risk:** Monotonic pending growth across clears would make each later cycle's submission list and snapshot strictly larger (O(files x cycles) again). Actual: both clear paths empty the set; the committed-prune removes entries; the submission list is rebuilt per cycle from live dirty + current pending. Monotone growth survives only in touchedFiles/changedFiles/gatePassedFiles ledgers, which act as membership filters in scope derivation and cannot add files to a submission on their own.
- **Fix:** None required.
- **Evidence:** Reset base2.ts:6581-6582 'pendingGateFiles.clear(); activeWorkState.pendingGateFiles = []' (gate pass) and 4496 + 4513 'activeWorkState.pendingGateFiles = []' / 'pendingGateFiles.clear()' (conversation-gate reuse); removal at 1950 'pendingGateFiles.delete(pendingFile)'. Per-cycle rebuild base2.ts:4468-4472 (deriveGateScopeFiles(gitStatusFiles) ∪ currentPendingGateFiles). Monotone-but-harmless ledgers: 9182-9187 push-if-absent into touchedFiles/changedFiles/pendingGateFiles, 11466 'gatePassedFiles.add(file)' — consumed only as filters in collectTaskRelatedFiles (11473-11481) and deriveGateScopeFiles (11538-11542).

## [LOW] api-contract — docs/request-flow.md:273 — Divergence CONFIRMED — docs claim a snapshot-fingerprint 'mismatch ... is blocking' but the shipped gate tolerates mismatch on full file coverage (drift recorded only); runtime-owned gate surfaces consume base2's published state and never rebuild the file lists
- **Risk:** A consumer trusting docs/request-flow.md:273-274 expects ANY fingerprint mismatch to block; the gate credits a coverage-complete review whose echoed (well-formed v3) fingerprint is stale or wrong and merely records drift, so a review of stale file content can pass with a telemetry note. Second asymmetry: the final code-reviewer spawn carries no machine-bound changed_files param (security does), so its file list is enforced only via prompt text plus the reviewedFiles attestation check. On the runtime-vs-in-repo split: this harness's runtime-owned gate pieces (tool-executor git-committer guards, run_targeted_validation snapshot checks) consume uncommittedUnvalidatedFiles/<gate-state> published by base2's handleSteps; per-cycle file-list construction exists ONLY in base2's in-repo handleSteps gate — no second list builder to diverge.
- **Fix:** Either tighten collectReviewerAttestationIssues to require exact expectedFingerprint equality, or amend docs/request-flow.md:273-274 to describe the coverage-complete tolerance and the collectReviewerFingerprintDrift record. Give the final code-reviewer spawn params.changed_files / snapshot_fingerprint for parity with securitySpawnFiles (base2.ts:2885-2888).
- **Evidence:** docs/request-flow.md:273-274: 'Versioned reviewer results must echo the exact snapshot fingerprint and attest to every pending file. A mismatch or omitted file is blocking.' vs agents/base2/gate-reviewer.ts:289-295: 'Fingerprint tolerance: a coverage-complete review reporting a well-formed v3 fingerprint is trusted even when the exact snapshot id advanced between its spawn and attestation; only a FILE-COVERAGE gap or a missing / non-attestable fingerprint stays a hard blocker', and the blocking condition gate-reviewer.ts:302-306 'reportedFingerprint !== expectedFingerprint && missing.length > 0' (mismatch blocks ONLY with a coverage gap). Spawn-binding asymmetry: security base2.ts:2885-2888 'params: { changed_files: securitySpawnFiles, snapshot_fingerprint: securitySpawnFingerprint }' vs final code-reviewer spawn base2.ts:5327-5358 binding files only via prompt lines 5336-5339. Runtime-owned surfaces: docs/request-flow.md:305-320 (tool-executor gate-not-green + uncommitted-unvalidated guards consuming 'uncommittedUnvalidatedFiles' published by base2) and docs/request-flow.md around the Tool Call Lifecycle (targeted validation snapshot-ID checks); base2's publication filters to agent-touched files (touchedFiles/changedFiles/pendingGateFiles/gatePassedFiles) per docs/request-flow.md:316-319.

## Coverage receipt

### Subsystems
- base2-handleSteps-gate
- gate-reviewer-attestation
- gate-state-ledgers
- gate-runtime-docs

### Features
- incremental-gate-submission
- reviewer-attestation
- already-attested-ledger
- new-file-absorption
- committed-prune-credit
- gate-clear-reset
- review-receipt-skip

### Files
- agents/base2/base2.ts
- agents/base2/gate-reviewer.ts
- agents/base2/gate-concurrency.ts
- agents/base2/gate-files.ts
- docs/request-flow.md

### Domains
- api-contract
- correctness
- state-mutation
- performance
